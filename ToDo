- set preferred provider
- failover provider
  - validate email options
- need to log failures
- store metrics in database
- log metrics - num of emails sent by provider, num of failures
- can store email content as json blob
- swagger
- root / url expose routes
- /health-check
- /metrics route for debugging
- uncaughtException / 500 errors
- multiple environments
- custom Email Error class